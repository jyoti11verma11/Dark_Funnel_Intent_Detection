// The Dark Funnel dashboard is served as a plain HTML/JS single page from
// `public/index.html`. React is intentionally not used - this file exists only
// so the CRA dev server has something to mount into #root without erroring.
import "@/App.css";

function App() {
  return null;
}

export default App;
